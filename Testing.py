import random
number = random.randint(2, 12)
if int(number) < 10:
    number = " " + str(number)
for item in range(0, len(hexes):
    if hexes.numb = number:
        for p in players:
            if settle[]